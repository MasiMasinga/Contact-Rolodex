// Mui
import { styled } from '@mui/system';

const LogoContainer = styled('img')({
    width: '100%',
    height: '100px',
    objectFit: 'contain',
    position: "relative",
    right: "40%"
});

export default LogoContainer